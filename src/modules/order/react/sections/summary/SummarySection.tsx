import { Box, Typography } from '@mui/material';

export const SummarySection: React.FC<{}> = () => {
  return (
    <Box>
      <Typography variant="h5">Recapitulatif</Typography>
    </Box>
  );
};
